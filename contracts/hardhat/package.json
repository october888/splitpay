{
  "name": "splitpay-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat project for the SplitPay smart contract",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:arc-testnet": "hardhat run scripts/deploy.ts --network arcTestnet",
    "deploy:arc-mainnet": "hardhat run scripts/deploy.ts --network arcMainnet",
    "verify:sourcify": "hardhat verify",
    "console:arc-testnet": "hardhat console --network arcTestnet",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.13",
    "@types/node": "^22.10.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.17",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
